import '../styles/_header.scss'

const Header = () => {
    return (
        <header className="site-header">
            <h1>CSS & Sass Feature Preview</h1>
        </header>
    )
}

export default Header
